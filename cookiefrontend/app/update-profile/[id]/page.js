"use client";

// i need to dynamically pass the user._id drama from the token 



export default function UpdateProfilePage() { // Make sure it has 'export default'
  return (
    <div>
      <h1>Update Profile</h1>
    </div>
  );
}